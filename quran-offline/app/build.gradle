plugins {
    id 'com.android.application'
}

android {
    namespace 'com.mastermedia.quranoffline'
    compileSdk 35

    defaultConfig {
        applicationId 'com.mastermedia.quranoffline.lite'
        minSdk 26
        targetSdk 35
        versionCode 12
        versionName '4.8.0'
        resourceConfigurations += ['ar']
    }

    buildTypes {
        release {
            minifyEnabled true
            shrinkResources true
            zipAlignEnabled true
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }

    androidResources {
        // Audio must remain uncompressed so MediaPlayer can open it directly
        // from the APK through AssetFileDescriptor.
        noCompress += ['ogg']
    }

    packaging {
        resources {
            excludes += [
                    'META-INF/DEPENDENCIES',
                    'META-INF/LICENSE*',
                    'META-INF/NOTICE*',
                    'META-INF/*.kotlin_module'
            ]
        }
    }
}

dependencies {
}
