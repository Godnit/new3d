name: Android runtime test for City Quest 3D

on:
  workflow_dispatch:

jobs:
  waiting-for-mobile-apk:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Waiting for the new mobile-renderer APK artifact."
