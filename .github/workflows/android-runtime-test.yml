name: Android runtime test for City Quest 3D

on:
  workflow_dispatch:

jobs:
  waiting-for-v1-1-apk:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Waiting for the landscape APK with bundled 3D assets."
