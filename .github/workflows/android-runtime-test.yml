name: Obsolete City Quest Legacy Dispatcher

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "The legacy build now runs directly from build-android.yml."
