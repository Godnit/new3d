name: Monitor Corrected GLES2 Build

on:
  push:
    branches: [cityquest3d-legacy-tabs-v2]
  workflow_dispatch:

permissions:
  actions: read
  contents: write

jobs:
  monitor:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Capture branch workflow runs
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          sleep 25
          mkdir -p /tmp/cityquest-monitor
          gh run list --repo "${{ github.repository }}" --branch cityquest3d-legacy-tabs-v2 --limit 40 --json databaseId,name,status,conclusion,event,headSha,createdAt,url > /tmp/cityquest-monitor/RUNS.json
          printf '%s\n' "monitor_run_id=${{ github.run_id }}" "head_sha=${{ github.sha }}" > /tmp/cityquest-monitor/STATUS.txt
          cat /tmp/cityquest-monitor/RUNS.json

      - name: Publish run list
        run: |
          git config user.name "City Quest Monitor"
          git config user.email "actions@users.noreply.github.com"
          git checkout --orphan monitor-work
          git rm -rf . || true
          cp -a /tmp/cityquest-monitor/. .
          git add -A
          git commit -m "Publish corrected GLES2 run list ${{ github.run_id }}"
          git push origin HEAD:cityquest3d-monitor --force
