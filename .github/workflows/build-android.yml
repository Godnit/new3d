name: Build and test City Quest 3D APK

on:
  push:
    branches: [cityquest3d-build]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build-test:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - name: Checkout project
        uses: actions/checkout@v4

      - name: Install system tools
        run: |
          sudo apt-get update
          sudo apt-get install -y unzip zip wget xvfb libgl1

      - name: Download ready-made CC0 3D models
        run: |
          set -euxo pipefail
          rm -rf assets/kenney /tmp/city-models /tmp/car-models /tmp/character-models
          mkdir -p assets/kenney/{city,cars,characters}
          git clone --depth 1 https://github.com/immaculate-lift-studio/CityCrafter3D.git /tmp/city-models
          git clone --depth 1 https://github.com/Arslan12216775/kenney_car-kit.git /tmp/car-models
          git clone --depth 1 https://github.com/V-Sekai-fire/TEST_fbx_kenney.git /tmp/character-models
          cp -a "/tmp/city-models/addons/citycrafter/assets/example_assets/kenney_city-kit-commercial_20/Models/OBJ format"/. assets/kenney/city/
          cp -a /tmp/car-models/. assets/kenney/cars/
          cp -a /tmp/character-models/animated-characters-1/. assets/kenney/characters/
          rm -rf assets/kenney/cars/.git assets/kenney/characters/.git
          test "$(find assets/kenney/city -type f -iname '*.obj' | wc -l)" -gt 5
          test "$(find assets/kenney/cars -type f \( -iname '*.obj' -o -iname '*.glb' -o -iname '*.fbx' \) | wc -l)" -gt 0
          test "$(find assets/kenney/characters -type f \( -iname '*.obj' -o -iname '*.glb' -o -iname '*.fbx' \) | wc -l)" -gt 0
          echo "City models: $(find assets/kenney/city -type f -iname '*.obj' | wc -l)"
          echo "Car models: $(find assets/kenney/cars -type f \( -iname '*.obj' -o -iname '*.glb' -o -iname '*.fbx' \) | wc -l)"
          echo "Character models: $(find assets/kenney/characters -type f \( -iname '*.obj' -o -iname '*.glb' -o -iname '*.fbx' \) | wc -l)"

      - name: Apply compiler compatibility fixes
        run: |
          python3 - <<'PY'
          from pathlib import Path
          p = Path('scripts/main.gd')
          s = p.read_text()
          replacements = {
              'var move_input := hud.move_vector': 'var move_input: Vector2 = hud.move_vector',
              'var jump := hud.consume_jump() or Input.is_key_pressed(KEY_SPACE)': 'var jump: bool = hud.consume_jump() or Input.is_key_pressed(KEY_SPACE)',
              'var sprint := hud.sprint_held or Input.is_key_pressed(KEY_SHIFT)': 'var sprint: bool = hud.sprint_held or Input.is_key_pressed(KEY_SHIFT)',
              'var attack := hud.consume_attack() or Input.is_key_pressed(KEY_F)': 'var attack: bool = hud.consume_attack() or Input.is_key_pressed(KEY_F)',
              'var interact := hud.consume_interact() or Input.is_key_pressed(KEY_E)': 'var interact: bool = hud.consume_interact() or Input.is_key_pressed(KEY_E)',
              'var look := hud.consume_look()': 'var look: Vector2 = hud.consume_look()',
              'var instance := resource.instantiate()': 'var instance: Node = (resource as PackedScene).instantiate()',
          }
          for old, new in replacements.items():
              s = s.replace(old, new)
          p.write_text(s)
          for filename in ('scripts/player.gd', 'scripts/car.gd', 'scripts/enemy.gd'):
              p = Path(filename)
              s = p.read_text().replace('var instance := resource.instantiate()', 'var instance: Node = (resource as PackedScene).instantiate()')
              p.write_text(s)
          PY

      - name: Install Godot 4.6.3 and export templates
        run: |
          set -euxo pipefail
          wget -q --show-progress -O /tmp/godot.zip "https://github.com/godotengine/godot-builds/releases/download/4.6.3-stable/Godot_v4.6.3-stable_linux.x86_64.zip"
          wget -q --show-progress -O /tmp/templates.tpz "https://github.com/godotengine/godot-builds/releases/download/4.6.3-stable/Godot_v4.6.3-stable_export_templates.tpz"
          unzip -q /tmp/godot.zip -d /tmp/godot
          sudo install -m 0755 /tmp/godot/Godot_v4.6.3-stable_linux.x86_64 /usr/local/bin/godot
          mkdir -p "$HOME/.local/share/godot/export_templates/4.6.3.stable" /tmp/godot-templates
          unzip -q /tmp/templates.tpz -d /tmp/godot-templates
          cp -a /tmp/godot-templates/templates/. "$HOME/.local/share/godot/export_templates/4.6.3.stable/"
          godot --version

      - name: Generate Android debug signing key
        run: |
          keytool -genkeypair -v -keystore debug.keystore -storepass android -alias androiddebugkey -keypass android -dname "CN=Android Debug,O=City Quest,C=US" -keyalg RSA -keysize 2048 -validity 10000

      - name: Import all models and check scripts
        run: |
          set -o pipefail
          timeout 12m godot --headless --editor --path . --import 2>&1 | tee import.log
          if grep -E "SCRIPT ERROR|Parse Error|Failed to load script" import.log; then exit 1; fi
          timeout 45s godot --headless --path . --quit-after 180 2>&1 | tee smoke-headless.log || true
          if grep -E "SCRIPT ERROR|Parse Error|Failed to load script" smoke-headless.log; then exit 1; fi
          grep -q "CITYQUEST_READY" smoke-headless.log

      - name: Export signed Android APK
        run: |
          mkdir -p build
          godot --headless --path . --export-debug "Android" build/CityQuest3D.apk 2>&1 | tee export.log
          test -s build/CityQuest3D.apk
          unzip -t build/CityQuest3D.apk
          keytool -printcert -jarfile build/CityQuest3D.apk > build/signature.txt || true
          sha256sum build/CityQuest3D.apk | tee build/SHA256SUMS.txt

      - name: Install and launch on Android emulator
        uses: reactivecircus/android-emulator-runner@v2
        with:
          api-level: 29
          arch: x86_64
          profile: pixel_2
          disable-animations: true
          script: |
            set -euxo pipefail
            adb install -r build/CityQuest3D.apk
            adb logcat -c
            adb shell monkey -p com.godnit.cityquest3d -c android.intent.category.LAUNCHER 1
            sleep 22
            PID="$(adb shell pidof com.godnit.cityquest3d | tr -d '\r')"
            echo "App PID: $PID"
            test -n "$PID"
            adb exec-out screencap -p > build/android-emulator-screenshot.png
            adb logcat -d > build/android-logcat.txt
            if grep -E "FATAL EXCEPTION|Fatal signal|Process: com.godnit.cityquest3d.*has died" build/android-logcat.txt; then exit 1; fi

      - name: Package source with downloaded models
        run: |
          zip -qr build/CityQuest3D-Full-Source.zip . -x '.git/*' '.godot/*' 'build/*' 'debug.keystore' 'project-source.zip'

      - name: Upload diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: CityQuest3D-Diagnostics
          path: |
            import.log
            smoke-headless.log
            export.log
            build/android-logcat.txt
            build/android-emulator-screenshot.png
          if-no-files-found: ignore
          retention-days: 3

      - name: Upload ready files
        uses: actions/upload-artifact@v4
        with:
          name: CityQuest3D-Ready
          path: |
            build/CityQuest3D.apk
            build/CityQuest3D-Full-Source.zip
            build/android-emulator-screenshot.png
            build/android-logcat.txt
            build/SHA256SUMS.txt
            build/signature.txt
          if-no-files-found: error
          retention-days: 30
